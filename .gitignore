# the connectome itself: 540 MB of CC-BY feather files, fetched with `python run.py fetch`
data/
# the built graph (38 MB), per-generation gains snapshots and the mushroom-body state — rebuilt or regrown, not committed
build/
# the current evolved gains are committed now and then on purpose (assets/gains.npz)
# finished pieces live in gallery/ (full PNG + provenance) and site/recordings/ (score + 512 px copy);
# both are committed on purpose — they are the work
# the studio's own log and stop marker
studio.log
studio.stop
# never
.env
*.key
keystore*.json
# python / node
__pycache__/
*.pyc
.venv/
venv/
tools/node_modules/
tools/package-lock.json
# editors / os
.vscode/
.idea/
.DS_Store
Thumbs.db
